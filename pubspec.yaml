name: text_encrypter
description: "A new Flutter project."
publish_to: 'none'
version: 3.0.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.7.0
  archive: ^4.0.9
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
